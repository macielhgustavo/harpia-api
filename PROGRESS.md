# Progresso — Harpia API
- **[2026-09-21] — CRM-017, matching determinístico de unidades:** endpoint tenant-scoped e paginado consulta o perfil atual contra estoque disponível e preço em tabela ativa mais recente, com filtros rígidos de tenant/empreendimento/tipologia/disponibilidade e critérios suaves explicáveis de quartos, área e preço. Entrada e objetivo ficam `NOT_EVALUATED`; não há score percentual, IA, automação ou seleção automática. Uma consulta SQL parametrizada faz redução, ordenação e página sem N+1; migration aditiva indexa candidatos. No detalhe Angular, CTA sob demanda mostra resultados, critérios, unidade já selecionada, carregar mais e retry sem derrubar a página. CRM-018 e CRM-019 permanecem pendentes. Validação: 6 testes unitários focados, 64 suítes/393 testes backend completos, 5 specs/17 E2E em PostgreSQL real, 33 testes frontend focados e 544 completos; builds API/web, Prisma validate, ESLint e `git diff --check` aprovados.
- **[2026-09-21] — CRM-015, perfil de interesse imobiliário:** perfil opcional 1:0..1 independente da unidade selecionada, com empreendimento/tipologia desejados, faixas de quartos/área/preço, entrada, objetivo e observações. `GET/PUT/DELETE /crm/opportunities/:id/interest` fazem leitura e upsert/substituição/remoção sob tenant, permissões e auditoria transacional; a migration aditiva não reescreve oportunidades antigas. O detalhe Angular ganhou card e formulário inline com validação, estados próprios e leitura sem permissão de escrita. Matching, score e recomendações permanecem fora do escopo. Validação: 25 testes backend focados, 63 suítes/387 testes backend completos, 4 specs/9 E2E em PostgreSQL real, 68 testes frontend focados, 536 testes frontend completos, builds API/web e Prisma validate passaram; ESLint e `git diff --check` verificados no fechamento.
- **[2026-09-18] — CRM-052/BUG-10, ciclo comercial E2E em PostgreSQL real:** `npm run test:e2e` deixou de apontar para scaffold inexistente e agora cria PostgreSQL efêmero por Docker ou cluster nativo, valida agressivamente o destino, recria somente o schema de teste, aplica as 24 migrations com `prisma migrate deploy` e faz cleanup garantido. Três specs/6 testes exercitam login/JWT e contratos HTTP reais no fluxo pessoa → oportunidade → atividade → visita → unidade → reserva → proposta → aceite → venda; confirmam timeline cursorada, histórico, ganho centralizado/`stageEnteredAt`, lostReason após reabertura, Decimal, recebíveis, tenancy, RBAC `LEITURA`, reserva concorrente e rollback de conversão com unicidade violada. Factories diretas ficaram limitadas a tenant, credenciais e catálogo. Execução real local: Jest em ~5 s e comando completo com cluster/migrations/cleanup em ~50 s. A correção preexistente do spec de foco móvel foi isolada no frontend: o botão `md:hidden` era invisível no viewport desktop do Karma; o teste agora o torna focável e a suíte voltou a 517/517 sem mudar o produto.
- **[2026-09-18] — CRM-FIX-09, ciclo de estados de visitas validado no backend:** `VisitsService.update` passou a usar um plano de transição puro, calculado sobre a linha `SalesVisit` bloqueada por tenant na mesma transação. Somente `AGENDADA` pode ser editada/reagendada ou finalizada em `REALIZADA`, `NAO_COMPARECEU` e `CANCELADA`; os três destinos são terminais. Outcome/result só são aceitos na realização, ausência preserva `scheduledAt`, e cancelamento exige motivo e não admite resultado comercial (BUG-09). No-op real não escreve nem audita; concorrência observa o status atualizado e retorna `409` em vez de sobrescrever. A timeline paginada continua projetando a visita persistida e prioriza o motivo no cancelamento. As telas de agenda e detalhe só oferecem ações para agendadas e recarregam após conflito, preservando o erro. Nenhuma migration ou backfill. Validação: 53 testes backend focados, 62 suítes/362 testes completos, build, ESLint, Prisma validate e `git diff --check`; 52 specs frontend focadas, build e 511 testes fora de `AppComponent.spec.ts` passaram. A suíte integral frontend teve 516 aprovações e 1 falha preexistente de foco no menu móvel de `AppComponent.spec.ts`, reproduzida isoladamente e não alterada nesta tarefa. A antiga referência CRM-FIX-09 a E2E foi mantida como pendência em CRM-052/BUG-10.
- **[2026-09-18] — CRM-FIX-08, paginação do histórico e da timeline:** `GET /crm/opportunities/:id/history` usa `page/pageSize` e retorna `data/pagination`; `GET /timeline` usa `limit/cursor` com ordem total `occurredAt DESC, id ASC`. A consulta unificada seleciona apenas chaves das seis fontes com limite global antes de hidratar a página, preservando tipos, motivo histórico de perda, tenant e `CRM_READ`. O detalhe Angular carrega 20 itens inicialmente, anexa páginas anteriores com deduplicação, loading/erro/retry próprios e mescla eventos novos sem perder a fronteira antiga. Sem migration: as seis tabelas já têm índices por tenant/oportunidade. A ressalva de timestamps efetivos mutáveis está em `docs/crm-master-audit.md`. API: 28 testes focados, 61 suítes/343 testes completos, build, ESLint, Prisma validate e `git diff --check` passaram. Web: 47 specs focadas, 507 testes passando sem `AppComponent.spec.ts` e build. A suíte integral encontrou 1 falha de foco do menu móvel em `AppComponent.spec.ts:83` (arquivo não alterado; também falha isoladamente), com os outros 512 testes passando; não foi corrigida fora do escopo de CRM-FIX-08.
- **[2026-09-15] — CRM-FIX-07, remoção da relação morta de empresa em visitas:** `SalesVisit.companyId` foi confirmado como artefato de drift: apareceu no schema que criou visitas, ficou fora da migration e só foi materializado depois para reconciliar o Prisma, sem jamais ganhar writer, DTO, include, filtro, relatório, seed, SQL de negócio ou contrato frontend. A coluna e a relação foram removidas; a empresa/SPE passa a ser derivada exclusivamente por `SalesVisit.developmentId → Development.companyId`, com a unidade validada no mesmo empreendimento. A migration `20260915010000_remove_sales_visit_company_relation` é idempotente, aborta diante de qualquer valor não nulo inesperado e só então remove FK e coluna; não havia índice correspondente e nenhuma visita ou histórico é reescrito. A workspace não possuía banco local/de teste configurado, portanto nenhum banco foi consultado; a inspeção somente leitura obrigatória antes do deploy e a classificação de risco estão documentadas em `docs/crm-master-audit.md`. Seis testes foram adicionados e o teste da timeline foi ampliado, cobrindo criação com/sem empreendimento e unidade, derivação pela unidade, tenancy, listagem sem empresa, edição e auditoria. Backend validado com teste focado (28 casos), suíte completa de 60 suítes/334 testes, build, `prisma validate`, ESLint dos arquivos tocados e `git diff --check`. Frontend inalterado.

- **[2026-09-15] — CRM-FIX-06, preservação permanente do motivo de perda:** cada entrada em etapa perdida passa a gravar o motivo normalizado em `OpportunityStageHistory.lostReason`, nova coluna nullable criada por migration aditiva e sem backfill automático. `Opportunity.lostReason` permanece estado atual e é limpo ao reabrir ou ganhar, sem alterar eventos anteriores; perdas repetidas preservam motivos independentes. `applyOpportunityStageChange` continua como escritor único e agora também exige o motivo, limita-o a 500 caracteres, protege campos canônicos contra `additionalData`/`auditMetadata` e inclui o texto em `OPPORTUNITY_STAGE_CHANGED` e `OPPORTUNITY_LOST`; a sanitização final continua centralizada no `AuditService`. Histórico e timeline expõem o motivo, e o detalhe Angular o apresenta mesmo após reabertura. A recuperação de dados anteriores foi classificada e documentada, com consulta e script manual não executado. Backend validado com 60 suítes e 328 testes, build, `prisma validate` e ESLint dos arquivos tocados; frontend validado com 508 testes e build.

- **[2026-09-07] — CRM-FIX-05, documentação do ciclo de visitas na oportunidade:** o backend **não foi alterado**. Antes de escrever qualquer código, cada parte do ciclo pedido foi conferida contra os contratos existentes — listar por `opportunityId`, agendar derivando pessoa e tenant da oportunidade, agendar sem unidade e sem empreendimento via `resolveLocation`, reagendar por `PATCH` de `scheduledAt`/`durationMinutes`, concluir com `outcome` estruturado, registrar não comparecimento preservando `scheduledAt`, cancelar exigindo motivo, e a timeline que já agrega visitas. Nenhuma lacuna real de contrato foi encontrada, então nenhuma migration, DTO ou regra de domínio foi tocada e o BUG-05 se confirmou como lacuna exclusivamente de interface. Foram registradas em `docs/crm.md` as regras da nova seção de visitas do detalhe e, em `docs/crm-master-audit.md`, a resolução do BUG-05 com as limitações que permanecem: `UpdateSalesVisitDto` não aceita empreendimento nem unidade, de modo que reagendar não muda o local; o `PATCH` não possui máquina de estados, aceitando transições que a interface não oferece; e `GET /crm/visits` segue sem ordenação e sem filtro por empreendimento. Backend inalterado, com 59 suítes e 304 testes.

- **[2026-09-06] — CRM-FIX-04, paginação e agregados autoritativos no funil:** o Kanban deixou de carregar uma página do pipeline inteiro e distribuir as colunas no cliente. Criado `GET /crm/board`, que devolve cada etapa com página própria e agregados calculados sobre o conjunto filtrado completo: `total`, `loaded`, `hasMore`, `estimatedValue` e `weightedValue`. A agregação é uma única consulta agrupada por `(stageId, probability)`, o que permite calcular o valor ponderado sem SQL bruto e preserva uma só implementação dos filtros, agora em `buildOpportunityWhere` (`src/crm/opportunity-filters.ts`), reutilizada pela listagem, pelas páginas de cada coluna e pela agregação. Dinheiro é somado em `Prisma.Decimal` e serializado como string decimal; nada passa por `Number`. `stageLimit=0` devolve apenas agregados, usado para atualizar os totais após mover um card sem recarregar lista alguma. A ordenação de atividades ganhou `id` como desempate, sem o qual paginar poderia pular ou repetir registros. Migration aditiva e idempotente `20260906010000_crm_board_stage_index`, criando `Opportunity_organizationId_stageId_updatedAt_idx` para sustentar a ordenação das páginas por etapa; nenhum dado foi alterado. Backend com 59 suítes e 304 testes passando e build limpo.

- **[2026-09-06] — CRM-FIX-03, visões de tarefas mutuamente exclusivas (frontend):** `/crm/tasks` deixou de carregar uma única consulta de abertas e reclassificar no cliente. Cada aba passou a ser uma consulta própria a `GET /crm/activities`, aproveitando o contrato componível entregue pelo CRM-FIX-02: Hoje, Atrasadas e Próximas particionam a linha do tempo em `(-∞, hoje)`, `[hoje, amanhã)` e `[amanhã, +∞)`, e Concluídas usa `status=CONCLUIDA` sem `openOnly`. Uma atividade aberta pertence a exatamente uma das três visões, e uma atividade de hoje já passada permanece em Hoje. Os contadores das abas vêm do `pagination.total` da mesma consulta que produziu a lista, eliminando a dupla contagem. Ficou definida a política de fuso horário do CRM: o dia é o dia local do navegador, com limites em `setHours(0,0,0,0)`/`setDate(+1)` serializados em ISO, sem biblioteca nova. O backend não precisou de nenhuma alteração e segue com 58 suítes e 290 testes passando. No frontend, `crm-tasks.component.spec.ts` é o primeiro teste de componente de uma tela de CRM: 28 casos com relógio fixo, que falham em quatro pontos se as causas originais forem reintroduzidas. Suíte do frontend com 448 testes passando e `ng build` limpo.

- **[2026-09-06] — CRM-FIX-02, filtros de atividade componíveis:** `status` e `openOnly` deixaram de competir pela mesma chave do predicado e passaram a ser filtros independentes combinados com E lógico. A resolução é uma interseção de conjuntos em `buildSalesActivityStatusFilter`, e o predicado inteiro da listagem é montado por `buildSalesActivityWhere`, ambos em `src/crm/sales-activity-filters.ts`, eliminando a dependência da ordem de spread. `OPEN_SALES_ACTIVITY_STATUSES` passou a ser a fonte única da definição de atividade aberta. `status=CONCLUIDA&openOnly=true` agora devolve conjunto vazio em vez de atividades abertas; listagem e contagem compartilham o mesmo `where`, então a paginação não diverge do conjunto retornado. O contrato HTTP não mudou e nenhuma chamada do frontend envia os dois filtros juntos, então nenhuma tela mudou de comportamento. Vinte testes validam o predicado gerado e o resultado retornado; com a semântica antiga reintroduzida, oito deles falham. Build limpo e suíte completa com 58 suítes e 290 testes passando.

- **[2026-09-06] — CRM-FIX-01, consistência do marco de etapa:** `Opportunity.stageId` passou a ter um escritor único, `applyOpportunityStageChange` em `src/crm/opportunity-stage.ts`, usado pela movimentação manual, pelo aceite de proposta e pela conversão em venda. A função grava `stageEnteredAt`, cria o registro de `OpportunityStageHistory` e devolve os eventos de auditoria numa só operação, impedindo que os três divirjam; colunas extras do chamador não conseguem sobrescrever os invariantes de etapa e a chamada é no-op quando a oportunidade já está na etapa de destino. Transações, locks `FOR UPDATE` tenant-scoped e o lote único de auditoria por transação foram preservados, e nenhum módulo Nest novo foi acoplado. Onze testes de regressão validam `stageEnteredAt` diretamente nos quatro caminhos; com o carimbo removido do escritor único, cinco deles falham. Build limpo e suíte completa com 57 suítes e 270 testes passando. O backfill dos registros já gravados entre 2026-09-04 e 2026-09-06 está documentado em `docs/crm-master-audit.md` e **não foi executado**.

- **[2026-09-06] — CRM fase A, re-auditoria (CRM-A01):** o CRM foi reauditado contra o código real dos dois repositórios (backend `cfcca47`, frontend `33fc566`), sem adicionar funcionalidade. Confirmou-se que `stageEnteredAt`, `SalesStage.defaultProbability`, o ciclo explícito de `SalesActivity` (status, prioridade, `reminderAt` e `result`), a entidade `SalesVisit` e a timeline unificada já existem no backend e têm interface correspondente no frontend, incluindo drag and drop, tempo na etapa, `/crm/tasks` e `/crm/visits`. Permanecem válidos os riscos anteriores de funil limitado a uma página no cliente, origem e motivo de perda em texto livre, outbox de notificações sem worker durável e legado financeiro em `Float`. Foram registrados dez bugs e inconsistências verificados no código, sendo o mais grave a não atualização de `stageEnteredAt` quando a oportunidade é ganha via aceite de proposta ou conversão em venda. Nenhuma violação de tenancy, RBAC ou auditoria foi encontrada no CRM. Verificações executadas: `npx jest` no backend (56 suítes, 259 testes, todos passando), `ng build` e `ng test` no frontend (420 testes passando). `docs/crm.md`, `docs/crm-master-audit.md` e `docs/crm/CRM_BACKLOG.md` foram atualizados com o estado real.

- **[2026-09-04] — CRM fase D, visitas imobiliárias:** criada entidade tenant-scoped de visita vinculada à oportunidade, pessoa, corretor, empreendimento e unidade. O módulo cobre agenda, duração, local, comparecimento, ausência, cancelamento com motivo, resultado estruturado e observações, com locks de atualização, auditoria e inclusão na timeline comercial.

- **[2026-09-04] — CRM fase C, atividades e timeline:** atividades comerciais agora possuem status explícito, prioridade, lembrete e resultado, com compatibilidade para registros concluídos antigos. Criação e atualização mantêm status e data de conclusão coerentes, a agenda pode ser filtrada por responsável, tipo, status, prioridade e período, e a oportunidade expõe uma timeline unificada de etapas, atividades, reservas, propostas e vendas.

- **[2026-09-04] — CRM fase B1, base do pipeline gerencial:** etapas comerciais agora possuem probabilidade padrão e oportunidades registram a data de entrada na etapa. A criação herda a probabilidade da etapa quando não houver valor manual; movimentações atualizam o marco temporal sob o lock transacional já existente. A migration preserva dados e recompõe o tempo de etapa a partir do histórico mais recente.

- **[2026-09-02] — Correção monetária:** adicionado módulo de ajuste monetário com índices, valores, políticas e ajustes de recebíveis, incluindo endpoints para prévia e aplicação, com validação de permissões e integração ao módulo de recebíveis.

- **[2026-09-02] — Cobrança automatizada:** adicionadas réguas configuráveis por dias antes/depois do vencimento, fila idempotente por parcela e data, processamento recorrente a cada 15 minutos, cancelamento automático para recebíveis quitados/cancelados e até três tentativas controladas. O envio usa Resend somente com credenciais server-side, chave de idempotência e histórico tenant-scoped; regras começam desativadas para evitar disparos acidentais.

- **[2026-09-02] — DRE gerencial:** adicionada apuração por competência ou caixa, com período configurável, comparação automática contra o intervalo anterior, margem líquida e detalhamento das despesas por categoria. A visão respeita tenant e filtros por empresa/SPE, empreendimento e centro de custo.

- **[2026-09-02] — Conciliação bancária:** criado livro de extrato tenant-scoped com importação idempotente por fingerprint, indicadores e filtros paginados. Sugestões exigem mesma conta, natureza, valor e janela de três dias; vínculo, desvínculo, ignorar e restaurar usam lock, constraints e auditoria. Estornos de pagamentos desfazem automaticamente a conciliação sem alterar o lançamento original.

- **[2026-09-01] — Núcleo financeiro geral:** adicionadas categorias configuráveis com defaults por organização, centros de custo vinculáveis a empresa/empreendimento, contas a pagar manuais e automáticas, pagamentos reversíveis e transações financeiras de entrada/saída. Retornos pendentes geram payable na incorporadora geral; comissões devidas geram payable da venda. Pagamento, baixa, movimento, sincronização da origem e auditoria são atômicos. `GET /finance/summary`, `/finance/cash-flow` e `/finance/transactions` consolidam realizado/projetado com filtros por empresa, empreendimento e centro de custo; movimentações usam `Decimal(18,2)`, locks, constraints e paginação.

- **[2026-09-01] — Parcelas e contas a receber:** vendas agora geram recebíveis idempotentes a partir do plano contratado, dividindo centavos sem perda e calculando atraso sem persistir estado derivado. Pagamentos parciais/totais e estornos usam lock de linha, atualizam automaticamente a quitação da venda e, com o núcleo financeiro, criam/revertem a transação de caixa na mesma transação do banco e da auditoria. Foram adicionados filtros, resumo financeiro e migration com backfill das vendas existentes.

- **[2026-09-01] — Vendas imobiliárias formais:** a conversão de proposta aceita congela valores, compradores, plano e comissões; converte a reserva, vence a oportunidade, marca a unidade vendida e audita todos os efeitos na mesma transação. Listagem e detalhe são tenant-scoped, paginados e calculam saldo contratual; constraints, locks e idempotência protegem número, proposta e unidade contra conversão concorrente.

- **[2026-09-01] — Propostas comerciais versionadas:** propostas preservam versões imutáveis de preço, desconto e condições de pagamento, com envio, aceite, recusa e expiração auditados. O aceite revalida oportunidade, unidade e reserva sob lock; apenas uma versão aceita pode seguir para venda e estados calculados não são gravados indevidamente.

- **[2026-08-16] — Reservas comerciais de unidades:** Criado `UnitReservation` com estados `ATIVA`, `CANCELADA`, `EXPIRADA` e `CONVERTIDA`, vínculo opcional à oportunidade e rastreabilidade dos usuários responsáveis. Reserva, cancelamento, conversão e expiração atualizam a unidade e gravam auditoria específica na mesma transação. Lock tenant-scoped da unidade, revalidação e índice parcial impedem duas reservas ativas concorrentes. A expiração é normalizada em leituras/mutações e libera a unidade; a conversão mantém `RESERVADA` para o handoff de proposta/venda. O CRUD genérico não pode forçar esse status e exclusões preservam o histórico. Rotas usam `SALES_READ`/`SALES_WRITE`, filtros paginados e isolamento por organização.

- **[2026-08-16] — CRM comercial e gestão de oportunidades:** Criado o domínio tenant-scoped de pipelines, etapas configuráveis, oportunidades, histórico comercial de movimentações e atividades. O primeiro acesso cria com lock um pipeline padrão de oito etapas; novas oportunidades validam pessoa, responsável, empreendimento e unidade na organização, promovem a pessoa a `LEAD` e registram histórico e auditoria na mesma transação. O funil oferece busca, filtros e paginação limitada; movimentações validam a etapa no pipeline, exigem motivo de perda e diferenciam eventos de ganho/perda. Valores estimados usam `Decimal(18,2)` e atividades preservam a pessoa da oportunidade. As rotas usam `CRM_READ`/`CRM_WRITE`, locks e consultas tenant-scoped, com testes de atomicidade, RBAC e isolamento.

- **[2026-08-09] — Tipologias auditadas e contratos de empreendimento alinhados:** As mutações de tipologias agora recebem o ator autenticado e executam criação, edição, exclusão e auditoria na mesma transação. A exclusão direta registra também cada unidade que sobrevive sem tipologia; a exclusão de empreendimento registra as tipologias removidas pelo cascade. Locks tenant-scoped, ordem consistente entre tipologia e unidade e tratamento de corridas preservam o contrato 404 sem deadlocks de vínculo. Os DTOs passaram a normalizar e limitar nomes, rejeitar áreas negativas e aceitar `null` apenas para limpar campos numéricos no PATCH. O PATCH de empreendimento também aceita `null` para limpar datas previstas, distinguindo-o de campo ausente, e a API rejeita pares de lançamento/entrega invertidos inclusive em atualização parcial.

- **[2026-08-09] — Auditoria append-only e isolada por tenant:** Adicionada a migration `20260802030000_audit_logging` e o `AuditModule` global, com registro transacional de login, troca/reset de senha, convites, alterações de usuários, empresas, empreendimentos, unidades, tabelas/preços, investimentos, alocações, retornos, documentos privados e exportações. Os endpoints `GET /audit-logs` e `GET /audit-logs/:id` exigem `AUDIT_READ`, aplicam organização em todas as consultas e oferecem filtros e paginação limitada a 100 itens. Metadados são allowlisted e sanitizados recursivamente com limites de profundidade/tamanho; senhas, JWTs, tokens, hashes, headers, arquivos, binários e requests inteiros não são persistidos. Não existem rotas de alteração ou remoção dos registros.

- **[2026-08-02] — Convites seguros de usuários:** Adicionada a migration `20260802020000_secure_user_invitations` com tenant, papel, hash SHA-256 único, convidante, expiração e estados mutuamente exclusivos de aceite/revogação. Criados endpoints administrativos tenant-scoped para criar, listar e revogar convites e o endpoint público com throttling próprio para aceite. Criação, cadastro e aceite compartilham lock transacional por e-mail normalizado; o aceite é atômico, de uso único e deriva organização/e-mail/papel exclusivamente do convite. Respostas usam projeção sem hash ou URL, conflitos públicos são genéricos, `ADMIN` não gerencia convite `OWNER`, e o notifier padrão é no-op com logs sanitizados. A seed foi adaptada à nova FK e testes cobrem concorrência, multi-tenant, RBAC, hashing, notifier e `429`.

- **[2026-08-02] — Controle de acesso por papéis:** Adicionada a migration `20260802010000_role_based_access_control` com os papéis `OWNER`, `ADMIN`, `FINANCEIRO`, `COMERCIAL`, `OPERACIONAL` e `LEITURA`, estado ativo, último login e datas do ciclo de convite/aceite. Contas legadas foram preservadas como `OWNER`; novos tenants nascem com um `OWNER`. Criados matriz central de 30 permissões, `@RequirePermissions`, guard global fail-closed e proteção de todas as rotas operacionais. Respostas genéricas ocultam investimentos, contas bancárias, contagens de alocação e documentos vinculados a investimentos de papéis não financeiros. O módulo de usuários lista e filtra contas com projeção segura, altera papel/status, revoga JWTs por `tokenVersion`, impede autodesativação, bloqueia alterações de `OWNER` por `ADMIN` e protege concorrentemente o último `OWNER` ativo por tenant.

- **[2026-08-02] — Endurecimento de autenticação e recuperação de senha:** Adicionada a migration `20260802000000_auth_security_password_reset` com `User.tokenVersion` e `PasswordResetToken` (hash SHA-256 único, expiração, uso único, índices e cascade). Registro, login e recuperação normalizam e-mails; registro é transacional e serializado por e-mail normalizado. A política central exige senha forte somente em novos cadastros/trocas/resets, mantendo o login da seed legada. JWTs passam a validar usuário, organização e versão no banco; troca/reset incrementam a versão e revogam sessões anteriores. Criados endpoints de troca, solicitação e reset, notifier no-op sem envio/log de tokens, eventos estruturados saneados e limitação por IP apenas nas quatro rotas públicas de auth. Testes cobrem política, hashing de recovery token, login legado, não enumeração, reset, invalidação de JWT, throttling e o contrato de JWT dos relatórios.

- **[2026-08-02] — Relatórios financeiros exportáveis:** Criado ReportsModule com relatórios de captação, retornos previstos/realizados, retornos em atraso e posição por investidor em XLSX e PDF. As consultas são isoladas por organização, validam filtros de investidor e empreendimento, limitam períodos e resultados e reutilizam a regra de status calculado de retorno. Os exportadores geram buffers em memória, aplicam formato monetário brasileiro, proteção contra formula injection no Excel, paginação no PDF e `Cache-Control: no-store`. Testes cobrem consolidação monetária, ausência de duplicação, limites, validação de filtros, regra de atraso, exportação e cabeçalhos HTTP.

- **[2026-08-01] — Armazenamento privado de documentos:** DocumentsModule passou a usar StorageModule com drivers local e S3-compatible (AWS SDK v3), chaves UUID não previsíveis, validação de MIME e assinatura de PDF/DOCX/XLSX/PNG/JPEG, limite de 25 MB e compensação do objeto quando o insert falha. Criada a migration `20260801000000_add_private_document_storage` com `storageKey`, provider, nome original, MIME e tamanho, preservando `fileUrl` como endpoint autenticado de download. Removida a publicação estática de `/uploads`; o download local é transmitido pelo backend e o S3 usa URL assinada temporária. Testes unitários cobrem upload, isolamento por organização, download, remoção, arquivo ausente e expiração da URL assinada.

- **[2026-07-11] — DashboardModule (migração do backend completa):** GET /dashboard reescrito para a nova arquitetura — captação (totalCaptado/Alocado/CaixaGeral, totalInvestidores), retornos (pendentes/atrasados/pagos com a lógica de atraso), empreendimentos e unidades por status, valorEmVendas (preço da tabela ativa das unidades VENDIDA), captacaoPorEmpreendimento e últimas interações. Tudo em Promise.all. tsconfig.build.json sem exclusões: todos os módulos migrados e compilando.

- **[2026-07-11] — Documents e Interactions:** Migrados para o novo schema. InteractionsModule aponta para Person (personId, antes investorId): CRUD ordenado por date desc, filtro ?personId, valida person na org. DocumentsModule com upload multipart (multer, 25MB, ./uploads): vínculos opcionais person/investment/unit/development validados na org, filtros por cada um, DELETE remove registro + arquivo do disco; upload órfão é removido se a validação falhar.

- **[2026-07-10] — ReturnsModule (retorno por alocação):** Substituído o antigo ReturnsModule; Return agora pendura na Allocation (allocationId). Filtros ?allocationId/?investmentId/?developmentId/?status; cada retorno inclui a alocação com development e o investidor. Mantida a lógica de ATRASADO automático (PENDENTE vencido reportado como ATRASADO, sem persistir — filtro por status considera o computado) e a exigência de realizedDate+realizedAmount ao marcar PAGO (400).

- **[2026-07-10] — Investments e Allocations:** Substituído o antigo InvestmentsModule. Investment não aponta mais para empreendimento; expõe allocatedAmount (soma das alocações COM empreendimento) e unallocatedAmount (caixa geral). POST valida que o investidor tem papel INVESTIDOR (400). Novo AllocationsModule (filtros ?investmentId/?developmentId) com a regra de orçamento: soma das alocações não pode exceder o valor do aporte (400 com o disponível). developmentId null = caixa geral. Cascades: delete de aporte → allocations → returns.

- **[2026-07-10] — Developments, UnitTypes, Units, PriceTables:** Substituído o antigo ProjectsModule pelo DevelopmentsModule (filtros ?status/?type/?companyId, detalhe com unitTypes/units/priceTables, delete bloqueia se houver alocação → 409). Criados UnitTypesModule e UnitsModule (ambos escopados por ?developmentId, com validação cruzada tipologia×empreendimento). PriceTablesModule com preço por unidade (Modelo B): POST /price-tables/:id/prices faz upsert do UnitPrice, + PATCH/DELETE /unit-prices/:id. Todos org-scoped.

- **[2026-07-10] — CompaniesModule e BankAccountsModule:** CRUD de Company (SPE/Incorporadora, filtro ?type, inclui developments e _count) — delete bloqueia empresa com empreendimentos (409). CRUD de BankAccount (filtro ?companyId, inclui nome da company; valida companyId da org no create/update → 400). Ambos registrados no AppModule.

- **[2026-07-10] — PeopleModule:** Substituído o antigo InvestorsModule pelo PeopleModule (Person + PersonRole). CRUD completo com filtro por papel e busca, documento único por organização, includes (roles/investments/interactions/documents), e endpoints de papéis (POST/DELETE /people/:id/roles). Delete bloqueia pessoa com investimentos (409). Módulos antigos ainda não migrados foram excluídos do build temporariamente.

- **[2026-07-10] — Novo seed (arquitetura de incorporadora):** Reescrito o `prisma/seed.ts` para o novo schema — org "Harpia Incorporadora", admin, 3 companies (1 incorporadora + 2 SPEs), 3 contas bancárias, 6 pessoas com papéis (João como INVESTIDOR+CLIENTE), 2 empreendimentos, 3 tipologias, 6 unidades com preços (Modelo B), e o fluxo Investment → Allocation (incl. caixa geral) → Return. Populado no Neon sem erro.
- **[2026-07-10] — Novo schema (arquitetura de incorporadora):** Reescrito o `schema.prisma` para o modelo de plataforma de gestão de incorporadoras — Person (com PersonRole), Company (SPE/Incorporadora), BankAccount, Development (evolução de Project), UnitType, Unit, PriceTable, UnitPrice, e o fluxo Investment → Allocation → Return. Migration `new-architecture` aplicada (banco resetado; só havia seed). 16 tabelas criadas.
